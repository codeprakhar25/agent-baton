{
  "name": "agent-relay",
  "version": "0.1.0",
  "description": "Cross-agent handoff tool: transfer work between Cursor, Claude Code, and Codex when limits are hit",
  "bin": {
    "relay": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup ",
    "dev": "tsup --watch",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^12.1.0",
    "@inquirer/prompts": "^5.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
